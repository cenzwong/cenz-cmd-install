# Preparing C compiler (gcc is the C compiler)
sudo apt install gcc
# or
sudo apt install build-essential

# Preparing C++ compiler (g++ is the C compiler)
sudo apt install g++
# or
$ sudo apt install build-essential

